<h1>投稿一覧</h1>

<ul class="notes">
  <% @notes.each do |note| %>
    <li>
      <%= note.user.name %>
      <span class="content">
        <%= link_to note.title, note_path(note.id) %>
      </span>
    </li>
  <% end %>
</ul>

<br>

